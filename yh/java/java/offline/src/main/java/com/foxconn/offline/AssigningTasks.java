package com.foxconn.offline;

import java.util.Map;
import java.util.TreeMap;

public class AssigningTasks {
    public void Assigning(int number, TreeMap<Integer, Map<String,Integer>> rules){
        for (int i = 0; i < rules.size(); i++) {
            Map map=rules.get(i);
            
        }
    }
}
